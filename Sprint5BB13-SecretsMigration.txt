================================================================================
Sprint 5B-B1.3 — SECRETS QUE PRECISARAO SER ATUALIZADOS
================================================================================

Data:   2026-08-04
NENHUMA alteracao foi realizada — apenas listagem, conforme exigido pela
Atividade 4.

--------------------------------------------------------------------------------
ATIVIDADE 4 — LISTA EXATA
--------------------------------------------------------------------------------

  Os Secrets abaixo precisarao ser ATUALIZADOS (valor substituido) no app
  `hub-granmarquise`, quando a migracao for autorizada. Mantida a
  convencao `S3_*` — E' A UNICA que o codigo real hoje le
  (`resolveStorageConfig()` em src/backup-adapter.js, prioridade sobre
  `AWS_*`/`BUCKET_NAME`) — ver nota sobre `BACKUP_WRITE_*` no
  Sprint5BB13-BucketMigrationPlan.txt, Atividade 1, item 3.

    1. S3_BUCKET
       De:   hub-prod-backups
       Para: hub-portal  (ou o nome efetivamente criado, se
             `hub-portal` estiver indisponivel — ver
             Sprint5BB13-BucketMigrationPlan.txt, Riscos)

    2. S3_ACCESS_KEY_ID
       De:   credencial atual (fingerprint tid_...KCeW, len=54) — a
             MESMA credencial hoje usada para o bucket de validacao do
             hub-sdk; nao deve ser reaproveitada (ela nao tem escopo
             restrito ao novo bucket).
       Para: nova Access Key, escopada exclusivamente ao bucket
             `hub-portal`, com a policy definida em
             Sprint5BB13-IAMPolicy.txt (Put/Get/List, sem Delete).

    3. S3_SECRET_ACCESS_KEY
       De:   credencial atual (len=75)
       Para: Secret Key correspondente a` nova Access Key do item 2.

  Os Secrets abaixo PODEM PRECISAR ser atualizados, dependendo de como o
  bucket novo for provisionado:

    4. S3_ENDPOINT
       Valor atual: https://t3.storage.dev
       Acao: MANTER, salvo se o processo de criacao do novo bucket
       (Atividade 5) resultar num endpoint Tigris diferente — ambos
       `t3.storage.dev` e `fly.storage.tigris.dev` sao dominios validos
       da Tigris; confirmar qual o bucket novo usa antes de decidir.

    5. S3_REGION
       Valor atual: auto
       Acao: MANTER — Tigris usa `auto` para todos os buckets
       (comportamento ja observado e documentado desde a Sprint 5B-B1.2).

--------------------------------------------------------------------------------
SECRETS QUE NAO SERAO TOCADOS POR ESTA MIGRACAO (fora de escopo)
--------------------------------------------------------------------------------

  BUCKET_NAME, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,
  AWS_ENDPOINT_URL_S3, AWS_REGION — continuam AUSENTES (mecanismo
  LEGADO permanece desativado, achado da Sprint 5B-B1.1; corrigir isso e'
  uma decisao separada, nao coberta por este plano de migracao de
  bucket do Shadow Mode).

  BACKUP_ENGINE — continua AUSENTE. Esta migracao NAO ativa o Shadow
  Mode; apenas corrige o bucket/credencial que o Shadow Mode usaria
  QUANDO for ativado (Sprint 5B-B1.1, ainda pendente de GO).

  Todos os demais Secrets do app (SSO_SECRET, WEBHOOK_SECRET,
  CHAMADOS_URL, etc.) — sem qualquer relacao com backup/Tigris, fora de
  escopo.

================================================================================
FIM DO DOCUMENTO — Sprint5BB13-SecretsMigration.txt
================================================================================
